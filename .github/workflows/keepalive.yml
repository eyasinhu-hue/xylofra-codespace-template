# Keepalive disabled - not needed for on-demand GitHub-hosted runners
# Use agent-run.yml for on-demand command execution
# This file kept for reference only

name: keepalive-disabled

on:
  workflow_dispatch: {}

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Use agent-run.yml workflow for on-demand execution"
